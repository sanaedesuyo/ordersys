spring:
  application:
    name: ordersys
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/ordersys?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:ordersys}
    password: ${DB_PASSWORD:ordersys123}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

server:
  port: 8080
